intent:
  id: check_ntp_server_health
  workflow: workflows/check_ntp_server_health.yaml
  risk: low
  enforce_declared_modes: true
  modes: [read_only, dry_run]
  facts_contract: tecrax.ntp_server_observation@1.0
  catalog:
    title: Check NTP server observation
    summary: Observe bounded local NTP daemon variables without retaining peer identities or addresses.
    target_kinds: [host]
    required_capabilities: [ssh_readonly, tecrax.ntp.server_observation.v1]
    side_effect_class: none
    validation_ref: validation_rules/check_ntp_server_health.yaml
    runbook_ref: https://github.com/rozmiarD/tecrax/blob/main/docs/runbooks/ubuntu-host-readonly-runbook.md
