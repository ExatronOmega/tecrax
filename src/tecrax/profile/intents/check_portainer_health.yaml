intent:
  id: check_portainer_health
  workflow: workflows/check_portainer_health.yaml
  risk: low
  enforce_declared_modes: true
  modes:
    - read_only
    - dry_run
  catalog:
    title: Check Portainer health
    summary: Read the bounded unauthenticated status endpoint through verified TLS.
    target_kinds: [host]
    required_capabilities: [http_api]
    side_effect_class: none
    validation_ref: validation_rules/check_portainer_health.yaml
    runbook_ref: https://github.com/rozmiarD/tecrax/blob/main/docs/ubuntu-host-readonly-runbook.md
