intent:
  id: check_adguard_health
  workflow: workflows/check_adguard_health.yaml
  risk: low
  enforce_declared_modes: true
  modes:
    - read_only
    - dry_run
  facts_contract: tecrax.adguard_reachability@1.0
  catalog:
    title: Check AdGuard health
    summary: Verify bounded DNS resolution and web reachability without management credentials.
    target_kinds: [host]
    required_capabilities: [local_shell_readonly, tecrax.host.adguard_reachability.v1]
    side_effect_class: none
    validation_ref: validation_rules/check_adguard_health.yaml
    runbook_ref: https://github.com/rozmiarD/tecrax/blob/main/docs/runbooks/ubuntu-host-readonly-runbook.md
