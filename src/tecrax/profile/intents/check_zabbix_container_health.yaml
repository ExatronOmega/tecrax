intent:
  id: check_zabbix_container_health
  workflow: workflows/check_zabbix_container_health.yaml
  risk: low
  enforce_declared_modes: true
  modes:
    - read_only
    - dry_run
  catalog:
    title: Check Zabbix endpoint health
    summary: Verify bounded Zabbix API reachability without authenticated management access.
    target_kinds: [host]
    required_capabilities: [http_api]
    side_effect_class: none
    validation_ref: validation_rules/check_zabbix_container_health.yaml
    runbook_ref: https://github.com/rozmiarD/tecrax/blob/main/docs/ubuntu-host-readonly-runbook.md
