intent:
  id: restart_zabbix_agent
  workflow: workflows/restart_zabbix_agent.yaml
  risk: medium
  modes:
    - apply
  catalog:
    title: Restart Zabbix agent
    summary: Legacy fixture-only mutating workflow; unavailable to current read-only operator targets.
    target_kinds: [virtual_machine]
    required_capabilities: [mutating_connector]
    side_effect_class: service_restart
    validation_ref: validation_rules/restart_zabbix_agent.yaml
    runbook_ref: https://github.com/rozmiarD/tecrax/blob/main/docs/operation-catalog.md#restart-zabbix-agent
