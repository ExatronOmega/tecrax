intent:
  id: check_docker_services_health
  workflow: workflows/check_docker_services_health.yaml
  risk: low
  enforce_declared_modes: true
  modes:
    - read_only
    - dry_run
  catalog:
    title: Check Docker service health
    summary: Observe Docker service and socket unit state without Docker socket access or container inspection.
    target_kinds: [host]
    required_capabilities: [ssh_readonly]
    side_effect_class: none
    validation_ref: validation_rules/check_docker_services_health.yaml
    runbook_ref: https://github.com/rozmiarD/tecrax/blob/main/docs/ubuntu-host-readonly-runbook.md
